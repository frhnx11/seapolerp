import { AllottedTrucksScreen } from "@/features/allotted-trucks/allotted-trucks-screen";

/** Global allotted-trucks pool — editable (admin-guarded by the /admin layout). */
export default function AdminAllottedTrucksPage() {
  return <AllottedTrucksScreen />;
}
